const ConfigCompile = {
  VERBOSE_COMPILE: true,
  PROFILER_COMPILE: true,
  CLEAR_SCREEN: true,
  OPEN: true,
  PORT: 3000,
  BUILD_DIRNAME: "dist",
  PUBLIC_DIRNAME: "public",
}

module.exports = ConfigCompile
